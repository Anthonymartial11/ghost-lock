/* gmail-config.js — YOUR Google app key (Client ID).
 *
 * This is the public identifier of the OAuth app you create in Google Cloud
 * (console.cloud.google.com). It is safe to publish — it is not a secret.
 * What protects your Gmail is Google's own sign-in + the fact that only YOU
 * are listed as a test user of your app.
 *
 * Paste your Client ID between the quotes (ends in .apps.googleusercontent.com)
 * and redeploy. Empty = the inbox-scan feature shows setup instructions.
 */
window.GMAIL_CONFIG = {
  clientId: ''
};
